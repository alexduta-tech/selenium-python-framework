[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "smoke"
]
addopts = "--json-report --json-report-file=reports/report.json --html=reports/report.html --self-contained-html --show-capture=log"